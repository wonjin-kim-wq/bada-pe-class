# Behavioral Rules

- After making modifications to the codebase or satisfying a user's request, automatically run `git add .`, `git commit` with an appropriate message, and `git push`. Do not ask for the user's permission to do this.
